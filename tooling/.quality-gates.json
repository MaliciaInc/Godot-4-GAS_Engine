{
  "loc": {
    "max_file_lines": 450,
    "max_function_lines": 120,
    "include_extensions": [
      ".gd",
      ".py",
      ".ps1"
    ],
    "exclude_globs": [
      "addons/gut/**",
      "docs/**"
    ],
    "file_length_allowlist": [
      {
        "glob": "addons/GAS_Engine/components/ability_system_component.gd",
        "max_lines": 600,
        "reason": "The facade is the single public surface over six runtimes: one-line delegation, no branching, no state of its own. Its length tracks how much the engine can do rather than how tangled it is, and the file limit exists to catch the second. Splitting it would give callers two places to look for one component, which is the thing the facade was introduced to prevent. Raised from 520 to 600 at Task 9, when the handle/query API (get_active_effect, find_active_effects, count_active_effects, remove_active_effects and five more) added another eleven lines of the same one-line-delegation shape the 520 raise already accepted - every later task's own facade surface (T10 immunities, T11 ongoing requirements, and on) is the same pattern, not a new one, so the ceiling is set with headroom for it rather than re-litigated at each task."
      },
      {
        "glob": "addons/GAS_Engine/abilities/gameplay_ability.gd",
        "max_lines": 470,
        "reason": "Raised from the general limit for `completed()`, the ability-side counterpart of GameplayAbilityTask.completed(). try_activate() returns when activation BEGINS, so an ability that refuses a cost or has nothing to aim at has already ended by the time a caller reads the result; awaiting ability_ended then waits for a signal that will not fire again and the caller stops for good. Found by a real game built on this addon, where a battler awaiting an ended ability stopped the battle on that battler's turn permanently. Four lines of guard and the paragraph a reader deleting it would need - the doc was already cut to that half before the ceiling was touched. The file is otherwise the ability contract itself, one paragraph per authoring field, and its length tracks how much an ability can declare rather than how tangled it is."
      },
      {
        "glob": "addons/GAS_Engine/effects/gameplay_effect_spec.gd",
        "max_lines": 470,
        "reason": "Task 21 made create_application_copy() propagate a context-payload copy failure as an explicit null spec rather than silently continuing with context=null - a real correctness fix (apply_effect_spec_to_target_result()'s existing null-spec guard is what turns it into a proper refusal), not new surface area. The file is already dense with one-paragraph-per-field 'why' documentation; trimming further would cut the documentation this project treats as load-bearing rather than the five lines of guard logic the fix actually needed. Set with the same small headroom T9's raise used, not re-litigated per task."
      },
      {
        "glob": "addons/GAS_Engine/effects/gameplay_effect_runtime.gd",
        "max_lines": 500,
        "reason": "Task 22 added GameplayEffectRefusalLog for the runtime debugger: a refusal_log field, a shared _refuse() helper every apply() refusal point now routes through so the debugger's history can never drift from what apply() itself returns, and report_refusal() recording an evaluator failure the same way. Real new observability surface, not restructuring - trimmed everywhere the new code itself had any fat (the stack-overflow logging moved into GameplayEffectStackingRuntime, where that refusal actually originates, rather than special-cased back here); what remains is the minimum each call site needs. Set with the same small headroom T9's and T21's raises used, not re-litigated per task. Raised from 480 to 500 when the removal announcement was fixed: clearing an effect's receipts was split out of _detach() into discard_receipts() so it runs AFTER active_effect_removed/gameplay_effect_removal_finished rather than before, which is what lets a subscriber - AbilityTaskFactory.wait_gameplay_effect_removed_matching() among them - filter the removed effect on granted_tags at all. Seventeen lines: a four-line function, its 'why', and the three call sites that now discard explicitly. The doc block was already cut to the half that a reader reordering these calls back would need."
      }
    ],
    "function_parameter_allowlist": [
      {
        "glob": "addons/GAS_Engine/gameplay_tag/gameplay_tag_inspector_plugin.gd",
        "function": "_parse_property",
        "reason": "Godot declares EditorInspectorPlugin._parse_property with seven parameters. Overriding it with fewer stops the override working, and the tag picker has no other entry point. The signature is the engine's, not this project's."
      }
    ]
  },
  "test_location": {
    "include_extensions": [
      ".gd",
      ".py",
      ".ps1"
    ],
    "allowed_directories": [
      "test",
      "tests",
      "tooling/gates/tests"
    ],
    "test_file_globs": [
      "test_*.gd",
      "*_test.gd",
      "test_*.py",
      "*_test.py"
    ],
    "require_test_directory": true,
    "exclude_globs": [
      "addons/gut/**",
      "docs/**"
    ]
  },
  "magic_strings": {
    "include_extensions": [
      ".gd",
      ".py",
      ".ps1"
    ],
    "minimum_length": 3,
    "minimum_occurrences": 2,
    "minimum_distinct_files": 2,
    "fail_on": [
      "repeated",
      "templates",
      "colors"
    ],
    "allow_literals": [
      {
        "literal": "__main__",
        "reason": "Python's own entry-point guard, not a value this project chose."
      },
      {
        "literal": "export",
        "reason": "A GDScript keyword the emitters write and the gates' grammar tables list."
      },
      {
        "literal": "function",
        "reason": "A structural word in both the LOC gate's regions and its report vocabulary."
      },
      {
        "literal": "test",
        "reason": "Names the test category in the gates and the test directory in the policy."
      },
      {
        "literal": "res://addons/GAS_Engine/icons/gas_engine_asc.svg",
        "reason": "@icon() annotation. GDScript requires a parse-time literal here: Godot rejects a constant with \"isn't a constant expression\", so naming this value is not expressible in the language."
      },
      {
        "literal": "res://addons/GAS_Engine/icons/gas_engine_tags.svg",
        "reason": "preload() of an icon. GDScript requires a parse-time literal here: Godot rejects a constant with \"isn't a constant expression\", so naming this value is not expressible in the language."
      },
      {
        "literal": "res://addons/GAS_Engine/icons/gas_engine_icon_star.svg",
        "reason": "preload() of an icon. GDScript requires a parse-time literal here: Godot rejects a constant with \"isn't a constant expression\", so naming this value is not expressible in the language."
      },
      {
        "literal": "res://addons/GAS_Engine/managers/gameplay_cue_manager.gd",
        "reason": "The autoload's script: preload() in the ASC needs the type, and the plugin needs the same path to register the autoload. GDScript requires a parse-time literal here: Godot rejects a constant with \"isn't a constant expression\", so naming this value is not expressible in the language."
      },
      {
        "literal": "or_greater",
        "reason": "@export_range hint on three properties. GDScript requires a parse-time literal here: Godot rejects a constant with \"isn't a constant expression\", so naming this value is not expressible in the language."
      },
      {
        "literal": "class_name",
        "reason": "The addon emits GDScript and the Python gates describe GDScript's grammar, so both mention this keyword. Two languages cannot share one constant."
      },
      {
        "literal": "const",
        "reason": "The addon emits GDScript and the Python gates describe GDScript's grammar, so both mention this keyword. Two languages cannot share one constant."
      },
      {
        "literal": "return",
        "reason": "The addon emits GDScript and the Python gates describe GDScript's grammar, so both mention this keyword. Two languages cannot share one constant."
      },
      {
        "literal": "pass",
        "reason": "The addon emits GDScript and the Python gates describe GDScript's grammar, so both mention this keyword. Two languages cannot share one constant."
      },
      {
        "literal": "--list",
        "reason": "One contract spelled on both sides of a language boundary: the Python gates declare it and tooling/verify.ps1 consumes it. PowerShell cannot import a Python constant."
      },
      {
        "literal": "PASS",
        "reason": "One contract spelled on both sides of a language boundary: the Python gates declare it and tooling/verify.ps1 consumes it. PowerShell cannot import a Python constant."
      },
      {
        "literal": "FAIL",
        "reason": "One contract spelled on both sides of a language boundary: the Python gates declare it and tooling/verify.ps1 consumes it. PowerShell cannot import a Python constant."
      },
      {
        "literal": "test_*.py",
        "reason": "One contract spelled on both sides of a language boundary: the Python gates declare it and tooling/verify.ps1 consumes it. PowerShell cannot import a Python constant."
      },
      {
        "literal": "python",
        "reason": "Names the interpreter to launch in verify.ps1 and the language in the gates' catalog."
      },
      {
        "literal": "git",
        "reason": "The executable verify.ps1 runs and the discovery backend the gates name."
      },
      {
        "literal": "line",
        "reason": "A finding's line number in the duplication gate and a text line in the strict-typing pass; unrelated meanings."
      },
      {
        "literal": "name",
        "reason": "An attribute's name, a project setting's name, and a captured region's name in three unrelated modules."
      },
      {
        "literal": "normal",
        "reason": "A RichTextLabel theme slot in the dashboard theme and a raycast hit's surface normal; unrelated meanings."
      },
      {
        "literal": "params",
        "reason": "A parameter list in two halves of the LOC gate, split so the gate stays under its own file limit."
      },
      {
        "literal": "loc",
        "reason": "The gate's own name, in the gate and in the runner's roster of gates."
      },
      {
        "literal": "State Management",
        "reason": "An inspector category two unrelated resources legitimately share. Renaming one to satisfy the gate would make the editor read worse."
      },
      {
        "literal": "gates",
        "reason": "tooling/_gatelib.py must name the gates directory to put it on sys.path, and gate_io declares the same segment as a constant once inside it. A bootstrap cannot use the constant it bootstraps. Every other tooling spelling of this word was collapsed into that constant."
      },
      {
        "literal": "res://addons/GAS_Engine/cues/gameplay_cue_entry.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. A clean checkout proved it with 'Parser Error: Could not find type \"GameplayCuePoolBucket\"'. So the reference must be a preload, and preload takes a literal. Two constraints, no expressible fix. tooling/project_invariants.py now enforces the rule that makes this literal necessary."
      },
      {
        "literal": "res://addons/GAS_Engine/cues/gameplay_cue_notify.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. A clean checkout proved it with 'Parser Error: Could not find type \"GameplayCuePoolBucket\"'. So the reference must be a preload, and preload takes a literal. Two constraints, no expressible fix. tooling/project_invariants.py now enforces the rule that makes this literal necessary."
      },
      {
        "literal": "res://addons/GAS_Engine/cues/gameplay_cue_params.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. A clean checkout proved it with 'Parser Error: Could not find type \"GameplayCuePoolBucket\"'. So the reference must be a preload, and preload takes a literal. Two constraints, no expressible fix. tooling/project_invariants.py now enforces the rule that makes this literal necessary."
      },
      {
        "literal": "res://addons/GAS_Engine/target_data/gameplay_effect_context.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. A clean checkout proved it with 'Parser Error: Could not find type \"GameplayCuePoolBucket\"'. So the reference must be a preload, and preload takes a literal. Two constraints, no expressible fix. tooling/project_invariants.py now enforces the rule that makes this literal necessary. Two sites: the sibling that preloads it, and the file's own self-preload, which it needs because even its own class name is an unresolvable identifier at that point."
      },
      {
        "literal": "res://addons/GAS_Engine/target_data/gameplay_target_hit.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. A clean checkout proved it with 'Parser Error: Could not find type \"GameplayCuePoolBucket\"'. So the reference must be a preload, and preload takes a literal. Two constraints, no expressible fix. tooling/project_invariants.py now enforces the rule that makes this literal necessary. Two sites: the sibling that preloads it, and the file's own self-preload, which it needs because even its own class name is an unresolvable identifier at that point."
      },
      {
        "literal": "res://addons/GAS_Engine/utilities/project_settings.gd",
        "reason": "Inside the GameplayCueManager autoload's parse-time closure, where a global class name does not resolve: Godot initialises autoloads before it has scanned the project for class_name declarations. So the reference must be a preload, and preload takes a literal. Two sites: the sibling that preloads it, and the file's own self-preload, which it needs to qualify its EditorTagsTagEditorPropertyMatchType enum - a bare enum annotation binds to any global of that name in a consumer's project, and its own class name is an unresolvable identifier at that point, so neither the bare form nor the class-qualified one is available here."
      },
      {
        "literal": "res://addons/GAS_Engine/gameplay_tag/gameplay_tag_editor_property.gd",
        "reason": "A different reason from the autoload-closure entries above, and worth not collapsing into them: this file declares no class_name at all, so its `enum Shape` has no owner to be qualified with. A bare `Shape` annotation binds to whatever global named Shape a consumer's project happens to declare - the defect the sandbox found with Transition - so the enum needs an owner, and a self-preload is the only one available to a file with no class name. Two sites: the inspector plugin that preloads it, and that self-preload."
      },
      {
        "literal": "activation_required_query",
        "reason": "The same property named for two different jobs. GameplayAbilityDefinitionSnapshot lists it among the fields it captures, so drift on it can be reported; the asset validator uses it as the human-facing field path on a finding. Unifying them would mean naming all seventeen captured fields as constants to share two, which makes the capture list read worse than the repetition does - and the validator's label is a message to a designer, not a property lookup, so tying it to the lookup would couple two things that only look alike."
      },
      {
        "literal": "activation_blocked_query",
        "reason": "The same property named for two different jobs. GameplayAbilityDefinitionSnapshot lists it among the fields it captures, so drift on it can be reported; the asset validator uses it as the human-facing field path on a finding. Unifying them would mean naming all seventeen captured fields as constants to share two, which makes the capture list read worse than the repetition does - and the validator's label is a message to a designer, not a property lookup, so tying it to the lookup would couple two things that only look alike."
      },
      {
        "literal": "duplication",
        "reason": "The gate's own name, spelled in two files that mean different things by it: the runner uses it as the command name and the report basename, and the gate uses it as the policy section it reads. Giving either a different spelling to satisfy this rule would make the policy section harder to find than the repetition is to notice."
      },
      {
        "literal": "--exclude-standard",
        "reason": "The git flag that keeps ignored files out of a listing, spelled independently by engine_evidence.py and by gate_io.py. Both are their own git callers - engine_evidence.py is a standalone script outside the gates shared plumbing, and importing that plumbing would drag the gates CLI into a script that is not one."
      },
      {
        "literal": "*.gd",
        "reason": "The pathspec engine_evidence.py hands git to list the scripts the driven stages load, spelled independently of project_invariants.py's own scans. Both are standalone scripts outside the gates' shared plumbing, and neither can import from the other without dragging one script's concerns into the other's."
      },
      {
        "literal": "*.tscn",
        "reason": "The pathspec engine_evidence.py hands git for scenes, which the suite loads and must therefore fingerprint. It collides with a file-dialog filter in the cue manager tab - the same four characters meaning the same file kind for two entirely unrelated jobs."
      },
      {
        "literal": "RESULT:",
        "reason": "The first line of every gate receipt in this repository. verify.ps1 writes one per stage it runs, and engine_evidence.py writes the two the chain cannot run itself; the format belongs to the repository, so each writer spells it. A shared constant is impossible across PowerShell and Python."
      },
      {
        "literal": "--project-root",
        "reason": "product_identity.py is a standalone script with its own argparse, spelled independently of gate_io.py's shared CLI helper - it is not one of the gates that helper serves, so it cannot import the constant without importing the gates' own CLI plumbing for a script that isn't one."
      },
      {
        "literal": ".gd",
        "reason": "A GDScript file extension in languages.py's per-language table (comment/block style, for the LOC and duplication gates) and in product_identity.py's own flat text-suffix set (which files are worth scanning for legacy identity strings). Unrelated purposes; the table is not a general file-extension allowlist."
      },
      {
        "literal": ".godot",
        "reason": "The engine cache directory suffix in gate_io.py's default exclusions and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": ".import",
        "reason": "Godot's per-asset import-metadata extension in gate_io.py's default exclusions and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": ".json",
        "reason": "A file extension in run_gate.py's report-writer and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": ".md",
        "reason": "A file extension in run_gate.py's report-writer and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": ".ps1",
        "reason": "A file extension in languages.py's per-language table and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": ".py",
        "reason": "A file extension in languages.py's per-language table and in product_identity.py's own text-suffix set. Unrelated purposes."
      },
      {
        "literal": "addons/GAS_Engine",
        "reason": "The addon root path, spelled in product_identity.py's own ADDON_ROOT constant and independently in project_invariants.py's own project.godot check. The identity firewall cannot import project_invariants.py's constant without coupling two gates that check different files for different reasons."
      },
      {
        "literal": "addons/gut/",
        "reason": "The vendored test dependency's root, spelled in product_identity.py's own skip-prefix list and in project_invariants.py's own exclusion. Two independent scans of two different things, both needing to skip the same vendored tree."
      },
      {
        "literal": "artifacts/gates/",
        "reason": "The gate-receipt root, spelled in product_identity.py's own skip-prefix list and in tooling/verify.ps1's receipt path. A Python constant is not importable from PowerShell."
      },
      {
        "literal": "GAS_Engine",
        "reason": "The canonical product name, spelled in product_identity.py's own PRODUCT constant and in the addon's own ADDON_NAME constant in project_settings.gd. The identity firewall that verifies the addon's naming cannot import a constant from the addon it is verifying."
      },
      {
        "literal": "ls-files",
        "reason": "The git subcommand, spelled in product_identity.py's own subprocess call and in gate_io.py's own git helper. product_identity.py is a standalone script, not one of the gates gate_io.py serves."
      },
      {
        "literal": "project.godot",
        "reason": "The engine config filename, spelled in product_identity.py's own authority check and in project_invariants.py's own check. Two independent scripts checking the same file for different, unrelated properties."
      },
      {
        "literal": "res://",
        "reason": "The Godot resource-path prefix, spelled in product_identity.py's forbidden-vocabulary list and in project_invariants.py's own path checks. Unrelated purposes."
      },
      {
        "literal": "tooling/product_identity.py",
        "reason": "product_identity.py's own path, spelled once inside itself to exclude itself from its own tracked-file scan, and once in tooling/verify.ps1's stage-argument list. A Python constant is not importable from PowerShell."
      },
      {
        "literal": "await",
        "reason": "A GDScript keyword the gates' own grammar tables list, and the word the Ability Composer prints on a card that suspends the ability. Every card is glass, so the treatment cannot carry that meaning and the word has to - a label is read, a texture is only felt. Naming it anything else would make the card say something GDScript does not."
      },
      {
        "literal": "Tags",
        "reason": "The name of one of the engine's own subsystems. Any editor surface that lists them has to print it: the runtime debugger names the tab showing them, and the Composer's catalog names the palette group holding their nodes. Two surfaces printing the same subsystem name is the point rather than a duplication - renaming either would make one of them describe something the engine does not have."
      },
      {
        "literal": "Tasks",
        "reason": "The name of one of the engine's own subsystems. Any editor surface that lists them has to print it: the runtime debugger names the tab showing them, and the Composer's catalog names the palette group holding their nodes. Two surfaces printing the same subsystem name is the point rather than a duplication - renaming either would make one of them describe something the engine does not have."
      },
      {
        "literal": "for",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "while",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "break",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "continue",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "breakpoint",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "elif",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "match",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "var",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "super",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "func",
        "reason": "A GDScript keyword. Any tool that reasons about GDScript has to name it: the gates' grammar tables list it to tokenise a file, and the Ability Composer's subset lists it to say whether a line of that shape can be drawn. Neither is quoting the other, and neither can use a different word - the language chose it."
      },
      {
        "literal": "add_tag",
        "reason": "The name of a method on AbilitySystemComponent. Two callers name it: the Dialogic bridge maps a command word onto it, and the Ability Composer's catalog offers it as a node. Neither invented the word and neither may use a different one - it is the engine's own API surface, and two places calling one method is not a duplicated value."
      },
      {
        "literal": "remove_tag",
        "reason": "The name of a method on AbilitySystemComponent. Two callers name it: the Dialogic bridge maps a command word onto it, and the Ability Composer's catalog offers it as a node. Neither invented the word and neither may use a different one - it is the engine's own API surface, and two places calling one method is not a duplicated value."
      },
      {
        "literal": "type",
        "reason": "A key in Godot's own dictionaries - the argument descriptor returned by get_script_method_list(), and the project-settings property info. Both are the engine's spelling, not a value this project chose."
      },
      {
        "literal": "class",
        "reason": "A key in Godot's own dictionaries. The Ability Composer's type system reads the class hierarchy out of ProjectSettings.get_global_class_list(), whose entries are spelled with these; the other occurrence is the same word in another engine-facing table. Neither is a value this project chose, and renaming one would only stop it matching what Godot returns."
      },
      {
        "literal": "path",
        "reason": "A key in Godot's own dictionaries. The Ability Composer resolves a receiver to the script it is by reading ProjectSettings.get_global_class_list(), whose entries are spelled with this key, and the project-invariants gate reads the same key out of project.godot's own structures. Neither chose the word; renaming it would simply stop reading Godot."
      },
      {
        "literal": "base",
        "reason": "A key in Godot's own dictionaries. The Ability Composer's type system reads the class hierarchy out of ProjectSettings.get_global_class_list(), whose entries are spelled with these; the other occurrence is the same word in another engine-facing table. Neither is a value this project chose, and renaming one would only stop it matching what Godot returns."
      },
      {
        "literal": "attribute",
        "reason": "A fragment the Ability Composer matches a method name against to work out which palette category it belongs in. The word is not a value: it is part of the engine's own naming, and the same word turning up elsewhere in the project is a different thing that happens to be spelled the same. Assigning a hundred methods to categories by hand would be a list that drifts from the methods; matching on the name is the rule that cannot."
      },
      {
        "literal": "tag",
        "reason": "A fragment the Ability Composer matches a method name against to work out which palette category it belongs in. The word is not a value: it is part of the engine's own naming, and the same word turning up elsewhere in the project is a different thing that happens to be spelled the same. Assigning a hundred methods to categories by hand would be a list that drifts from the methods; matching on the name is the rule that cannot."
      },
      {
        "literal": "target",
        "reason": "A fragment the Ability Composer matches a method name against to work out which palette category it belongs in. The word is not a value: it is part of the engine's own naming, and the same word turning up elsewhere in the project is a different thing that happens to be spelled the same. Assigning a hundred methods to categories by hand would be a list that drifts from the methods; matching on the name is the rule that cannot."
      },
      {
        "literal": "AbilitySystemComponent",
        "reason": "Two different facts that happen to be spelled the same. AbilitySystemLocator uses it as the name a component is conventionally given in a scene - a convention, and its own comment says so - while the Ability Composer uses it as the class whose public calls it offers. Tying one to the other would mean renaming the scene convention silently changed which class the palette reads."
      },
      {
        "literal": "Copy",
        "reason": "Three different things spelled the same. The Ability Composer's context menu offers it as a verb a person reads; the attribute set drafts append it to a name to mark a duplicate; neither invented the English word. Tying them together would mean renaming a menu item changed what a duplicated draft is called."
      },
      {
        "literal": "Remove",
        "reason": "A verb on the Ability Composer's context menu and, elsewhere, the name of an icon in the dashboard's theme. A menu label and a theme key are not the same kind of thing, and borrowing one for the other would make a change to the icon set rewrite the menu."
      }
    ],
    "allow_globs": [],
    "theme_globs": [
      "**/theme/**",
      "**/themes/**",
      "**/styles/**"
    ],
    "exclude_globs": [
      "addons/gut/**",
      "docs/**"
    ]
  },
  "duplication": {
    "pair_allowlist": [
      {
        "left": "addons/GAS_Engine/targeting/gameplay_*_request_*d.gd:<file>",
        "right": "addons/GAS_Engine/targeting/gameplay_*_request_*d.gd:<file>",
        "reason": "Godot's 2D and 3D physics are two separate servers with two separate type hierarchies and no common base. An addon that supports both has to mirror them, and the only way to write the pair once would be to pass the query parameters as Variant - a dynamic boundary in the middle of our own code, which is exactly what the typing rules forbid. The mirror is the design, not an oversight, and it is declared here so a future duplication that is an oversight still fails. These four request objects differ only in Vector2 against Vector3."
      },
      {
        "left": "addons/GAS_Engine/targeting/gameplay_targeting_service.gd:raycast_2d",
        "right": "addons/GAS_Engine/targeting/gameplay_targeting_service.gd:raycast_3d",
        "reason": "Godot's 2D and 3D physics are two separate servers with two separate type hierarchies and no common base. An addon that supports both has to mirror them, and the only way to write the pair once would be to pass the query parameters as Variant - a dynamic boundary in the middle of our own code, which is exactly what the typing rules forbid. The mirror is the design, not an oversight, and it is declared here so a future duplication that is an oversight still fails. PhysicsRayQueryParameters2D and 3D share no ancestor."
      },
      {
        "left": "addons/GAS_Engine/targeting/gameplay_targeting_service.gd:overlap_2d",
        "right": "addons/GAS_Engine/targeting/gameplay_targeting_service.gd:overlap_3d",
        "reason": "Godot's 2D and 3D physics are two separate servers with two separate type hierarchies and no common base. An addon that supports both has to mirror them, and the only way to write the pair once would be to pass the query parameters as Variant - a dynamic boundary in the middle of our own code, which is exactly what the typing rules forbid. The mirror is the design, not an oversight, and it is declared here so a future duplication that is an oversight still fails. CircleShape2D and SphereShape3D share no ancestor either."
      },
      {
        "left": "test/unit/test_targeting_2d.gd:*",
        "right": "test/unit/test_targeting_3d.gd:*",
        "reason": "The two suites ask the same questions of the two physics servers, which is the point: the 2D suite passing says nothing about whether a sphere sweep reports what a circle sweep does. Only the cross-dimension pairing is declared here - duplication inside either file is still caught."
      },
      {
        "left": "addons/GAS_Engine/effects/gameplay_effect_evaluator.gd:_translate_magnitude_status",
        "right": "addons/GAS_Engine/magnitudes/gameplay_attribute_based_magnitude.gd:_translate",
        "reason": "Two independent status-translation functions with unrelated vocabularies - GameplayMagnitudeResult.Status to AttributeEvaluationResult.Status, and AttributeCaptureResult.Status to GameplayMagnitudeResult.Status - that happen to share the shape of a short match/return with a fallback. GameplayMagnitudeResult deliberately keeps its own enum rather than reusing either neighbour's, exactly so a magnitude failure and a capture failure are never confused; merging these two functions into one would mean merging the two enums they translate between, coupling vocabularies this project keeps separate on purpose."
      },
      {
        "left": "test/unit/*.gd:*Case._init",
        "right": "test/unit/*.gd:*Case._init",
        "reason": "GDScript has no named-parameter constructor, so every use_parameters() case class - a plain value object naming one row of a parameterized test - writes the same mechanical `field = in_field` assignment per constructor argument. Two such constructors are never masked-similar because they share logic; they are masked-similar because both are the one way GDScript has to assign N constructor arguments to N fields. Splitting the fields across several smaller classes to dodge the shape would make the tests harder to read for a similarity score, not a real duplication."
      },
      {
        "left": "test/unit/test_gameplay_live_magnitudes.gd:test_live_instant_resolves_fresh_each_time_without_a_binding",
        "right": "test/unit/test_gameplay_live_magnitudes.gd:test_snapshot_persistent_contribution_does_not_change",
        "reason": "Deliberately parallel, the same way the 2D/3D targeting suites are: one asks what an INSTANT application with a LIVE capture reads after its source changes, the other asks what an INFINITE contribution with a SNAPSHOT capture reads after the same change - two magnitude policies answering the identical question side by side. Forcing a structural difference between them to dodge the similarity score would hide that the parallel is the point: a reader comparing the two should see the same shape with the one line that differs, not two independently-shaped tests that happen to prove related things. The LIVE captures moved to their own file when the magnitude suite passed the length gate; this pin followed them, because a pin that names a file the functions have left is dead policy that only announces itself by the gate going red."
      },
      {
        "left": "addons/GAS_Engine/effects/components/gameplay_effect_component_runtime_context.gd:<file>",
        "right": "addons/GAS_Engine/effects/components/gameplay_effect_component_removal_context.gd:<file>",
        "reason": "Task 8's own design calls for two DTOs here, not one: a runtime context and a removal context that happen to carry the same five fields today, kept separate because they answer different questions and a future field (a removal reason, an applied-vs-refreshed flag) belongs on only one of them. Collapsing them into a shared type the moment they match exactly would force every later addition to decide whether the other call site tolerates an unused field, coupling two call sites the framework deliberately keeps free to diverge."
      },
      {
        "left": "addons/GAS_Engine/effects/gameplay_effect_component_runtime.gd:_notify_runtime",
        "right": "addons/GAS_Engine/effects/gameplay_effect_component_runtime.gd:notify_removed",
        "reason": "Both walk the same components/states pairing, but one builds a GameplayEffectComponentRuntimeContext for on_effect_applied/on_effect_executed and the other a GameplayEffectComponentRemovalContext for on_effect_removed - the same two deliberately-separate DTOs declared above. Sharing one iterator between them would mean it either returns Variant or takes a context-building Callable per call site, in both cases hiding which context type a given notification actually builds behind a layer neither reader nor the type checker can see through."
      },
      {
        "left": "test/unit/test_gameplay_effect_stacking.gd:test_the_three_expiration_policies_govern_what_survives",
        "right": "test/unit/test_gameplay_effect_stacking.gd:test_turn_based_expiration_uses_remaining_turns_not_seconds",
        "reason": "Task 12's own doc lists these as two separate required tests: the same three StackExpirationPolicy values answering the identical question once for a DURATION effect's seconds and once for a TURN_BASED effect's remaining_turns - the parallel exists specifically to prove the policies never convert one unit into the other. Merging them into one parametrized test would hide that a duration-clock bug and a turn-clock bug are two distinct regressions this suite needs to catch independently."
      },
      {
        "left": "test/unit/test_smoke.gd:test_legacy_default_paths_migrate_to_gas_engine_defaults",
        "right": "test/unit/test_smoke.gd:test_legacy_custom_path_is_preserved_during_namespace_migration",
        "reason": "The GAS_Engine rebrand's own migration test requirement lists these as two separate cases: the same _migrate_legacy_project_settings() call answering the identical question once for a legacy setting still holding the old default path (it must translate to the new default) and once for a legacy setting holding a project-chosen custom path (it must survive untouched). Merging them into one parametrized test would hide that 'the default moved' and 'a custom path is not the default's problem' are two distinct regressions this suite needs to catch independently."
      },
      {
        "left": "test/unit/*.gd:_*_cases",
        "right": "test/unit/*.gd:_*_cases",
        "reason": "Same shape as the *Case._init wildcard above, one level up: every use_parameters() case-list builder is `var x = XCase.new(); x.field = ...; return [x, ...] as Array[XCase]`, because that is the only way GDScript builds a handful of named-field objects without a keyword constructor. Two builders are masked-similar because they are both that one mechanical shape, not because one was copied from the other - contorting a case list's construction to dodge the score would make the test harder to read for nothing real."
      },
      {
        "left": "test/unit/test_ability_tag_semantics.gd:test_activating_an_ability_cancels_others_matching_its_cancel_query",
        "right": "test/unit/test_ability_tag_semantics.gd:test_a_cancel_ability_tags_effect_cancels_matching_active_abilities",
        "reason": "Task 15's own design calls for cancel_matching_query to have exactly one implementation reached by two distinct callers: an ability's own cancel_abilities_query on a successful activation, and GameplayEffectCancelAbilityTagsComponent.on_effect_applied(). These two tests are deliberately parallel, the same way the 2D/3D targeting suites are - each grants a channelling ability, triggers the OTHER caller, and asserts the same cancellation - because the point is that both entry points reach the identical algorithm. Forcing a structural difference between them to dodge the score would hide that the parallel is the point: a reader comparing the two should see the same shape proving the same shared code path, not two independently-shaped tests that happen to prove related things."
      },
      {
        "left": "test/unit/test_ability_activation_policies.gd:test_passive_waits_for_its_required_tag",
        "right": "test/unit/test_ability_activation_policies.gd:test_passive_restarts_once_conditions_are_satisfied_again",
        "reason": "Deliberately parallel, not copied: both grant the same kind of gated passive and prove the first wake on a required tag - the restart test then continues past that exact point to prove the same passive can go idle and wake a second time, which is Task 16's own explicit requirement that a passive 'puede activarse de nuevo' once its conditions hold again. Collapsing them into one test would hide that 'wakes once' and 'wakes again after going idle' are two distinct regressions this suite needs to catch independently, the same reasoning already applied to the stacking suite's duration/turn-based expiration pair."
      },
      {
        "left": "addons/GAS_Engine/abilities/ability_lifecycle_runtime.gd:_translate",
        "right": "addons/GAS_Engine/effects/gameplay_effect_evaluator.gd:_translate_magnitude_status",
        "reason": "A third independent status-translation function, same family as the pair declared below: AbilityRuntime.ActivationError to GameplayAbilityActivationResult.Status here, GameplayMagnitudeResult.Status to AttributeEvaluationResult.Status there - unrelated vocabularies that happen to share the shape of a short match/return with a fallback. GameplayAbilityActivationResult.Status is Task 17's own closed, public-facing enum, deliberately not a re-export of ActivationError - merging this translator into either neighbour would couple an ability's internal gate reason to an unrelated public result contract."
      },
      {
        "left": "addons/GAS_Engine/abilities/ability_lifecycle_runtime.gd:_translate",
        "right": "addons/GAS_Engine/magnitudes/gameplay_attribute_based_magnitude.gd:_translate",
        "reason": "Same reasoning as the pair above, one level up: three status-translation functions across this codebase now share the mechanical match/return-with-fallback shape because that is the one way GDScript expresses a closed enum-to-enum mapping, not because any one was copied from another."
      },
      {
        "left": "test/unit/test_ability_lifecycle.gd:test_cancel_immediately_aborts_a_running_instance_and_drops_the_spec",
        "right": "test/unit/test_ability_lifecycle.gd:test_after_active_end_lets_a_running_instance_finish_first",
        "reason": "Deliberately parallel, the same way the 2D/3D targeting suites are: both grant and activate the same kind of channelling ability, then remove it - one under CANCEL_IMMEDIATELY, the other under AFTER_ACTIVE_END - and assert the opposite outcome for the still-running instance. The parallel is the point: AbilityRemovalPolicy's two values only mean something in contrast to each other, and collapsing these into one parametrized test would hide that 'aborted now' and 'left running until it ends on its own' are two distinct regressions this suite needs to catch independently."
      },
      {
        "left": "addons/GAS_Engine/abilities/tasks/*.gd:create*",
        "right": "addons/GAS_Engine/abilities/tasks/*.gd:create*",
        "reason": "Task 18's eight new observation tasks all follow the one factory shape `GameplayAbilityTask` itself already established: `var task = X.new(); task.owner_ability = ability; task.<fields> = <args>; task.target_asc = target if target != null else (ability.owner_asc if ability != null else null); return task`. Two `create()`/`create_matching()` functions are masked- or structural-similar because they are both that one mechanical shape GDScript has no named-parameter constructor to write once and share - not because one was copied from another. Forcing a structural difference between any two of them to dodge the score would make each task harder to read for nothing real, the same reasoning already applied to the *Case._init and _*_cases wildcards."
      },
      {
        "left": "addons/GAS_Engine/abilities/tasks/ability_task_wait_confirm_cancel.gd:create",
        "right": "addons/GAS_Engine/cues/gameplay_cue_params.gd:for_target",
        "reason": "Two unrelated two-field constructors - a task naming its confirm/cancel input slots, a cue param naming its instigator/target - that happen to share the shape of `var x = X.new(); x.a = a; x.b = b; return x`. Coincidence of shape, not of purpose: merging them would couple an ability task to a cue's authoring surface for no reason either was written."
      },
      {
        "left": "test/unit/test_ability_task_library.gd:test_wait_ability_activated_matches_by_handle",
        "right": "test/unit/test_ability_task_library.gd:test_wait_ability_ended_matches_by_handle",
        "reason": "The mirror of the production pair already declared above, one level up. AbilityTaskWaitAbilityActivated and AbilityTaskWaitAbilityEnded watch two different ASC signals - ability_activated and ability_runtime_ended - through the same filter, and the one way to prove either is: grant a watcher, a bystander and a watched ability, build the task, activate the bystander (must not wake it), activate the watched (must wake it). Both tests exist because each pair had exactly one half covered and the other half none at all, so collapsing them would put one of the two signals back to having no test - which is the defect these were written to close, not a shape to dodge."
      },
      {
        "left": "test/unit/test_ability_task_library.gd:test_wait_ability_ended_matches_by_tag_query",
        "right": "test/unit/test_ability_task_library.gd:test_wait_ability_activated_matches_by_tag_query",
        "reason": "The same pairing on the GameplayTagQuery half of both tasks, and the same reason: two different signals resolved through the same effective_ability_tags() filter, each needing its own proof. The by-query pair is worth keeping separate from the by-handle pair above for a further reason - the query path resolves the ended ability's tags through its spec, which the handle path never touches, so a spec that stops resolving would fail exactly one of the two."
      },
      {
        "left": "addons/GAS_Engine/abilities/tasks/ability_task_factory.gd:*",
        "right": "addons/GAS_Engine/abilities/tasks/ability_task_factory.gd:*",
        "reason": "Every factory in this file but `_own()` is `return _own(ability, X.create(ability, ...args)) as X` - the one wrapper shape the file exists to apply uniformly across Task 18's whole task library, declared in the file's own header comment. Two factories are masked-similar because they are both that one delegation, not because one was copied from another."
      }
    ]
  }
}
